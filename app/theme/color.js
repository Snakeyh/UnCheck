import { palette } from "./palette";

export const color = {
  palette,
  transparent: "rgba(0,0,0,0)",
  background: palette.black,
  accent: palette.orange,
  cards: palette.offWhite,
  primaryText: palette.black,
  secondaryText: palette.white
};
